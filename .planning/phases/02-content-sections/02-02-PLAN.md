---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/MissionSection.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - MISS-01
  - MISS-02
  - MISS-03

must_haves:
  truths:
    - "Hero section displays the logo, H1 headline, subtitle, date badge, and two CTA buttons"
    - "Hero has a blue gradient background (not 100vh — auto height with padding)"
    - "Mission section shows a two-column layout on desktop with text and blockquote left, fact cards right"
    - "Fact cards display the 3 key numbers from constants: 9 villes, ~3,000 km, 10 jours"
    - "Antoine's quote renders in a styled blockquote"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section component with logo, headline, date badge, CTAs"
      min_lines: 40
    - path: "src/components/sections/MissionSection.tsx"
      provides: "Mission section with two-column layout, quote, fact cards"
      min_lines: 40
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "public/rpt_logo.png"
      via: "next/image with priority"
      pattern: 'src="/rpt_logo.png"'
    - from: "src/components/sections/MissionSection.tsx"
      to: "src/lib/constants.ts"
      via: "FACT_NUMBERS import"
      pattern: "import.*FACT_NUMBERS.*from.*constants"
    - from: "src/components/sections/HeroSection.tsx"
      to: "#mission"
      via: "ghost CTA anchor link"
      pattern: 'href="#mission"'
---

<objective>
Build the Hero and Mission section components — the first two sections visitors see when they land on the page.

Purpose: These are the most important sections for the "understand in 30 seconds" core value. The hero communicates identity (what) and the mission communicates purpose (why).
Output: HeroSection.tsx and MissionSection.tsx, ready for page composition in Plan 05.
</objective>

<execution_context>
@/Users/dominikgrob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dominikgrob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@src/components/ui/Button.tsx
@src/components/ui/SectionWrapper.tsx
@src/lib/typography.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection component</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
    Create src/components/sections/HeroSection.tsx as a Server Component (no "use client" — no interactivity needed).

    **Layout (CONTEXT.md locked — centered stack like the poster):**
    - Do NOT use SectionWrapper — Hero needs its own full-width gradient background without SectionWrapper's default py-16/py-24 (hero padding is different)
    - Use a `<section id="hero">` with `bg-gradient-to-b from-brand-navy to-[#1e5faa]` and `text-white`
    - Inner container: `mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 flex flex-col items-center text-center`
    - Padding: `py-20 md:py-32` — compact auto height (NOT 100vh), next section peeks below
    - Vertical gap between elements: `gap-6` on flex container

    **Content stack (top to bottom):**
    1. Logo: `next/image` with `src="/rpt_logo.png"`, `width={424}`, `height={424}`, `priority` (LCP — HERO-05), `className="w-24 h-24 md:w-32 md:h-32 object-contain"`, `alt="Logo Rein Padel Tour"`
    2. Date badge (HERO-02): `<span>` with `inline-flex items-center rounded-full bg-brand-coral px-5 py-2 font-display text-sm font-bold uppercase tracking-widest` — text: `6 – 15 MARS 2026` (use en-dash, not hyphen)
    3. H1 (HERO-01): `font-display text-4xl font-black uppercase tracking-tight md:text-6xl lg:text-7xl` — text: `REIN PADEL TOUR 2026`
    4. Subtitle: `<p>` with `max-w-xl font-body text-lg text-white/80 md:text-xl` — text via Fr component: `"TOURNOIS AMICAUX CARITATIFS SUR TOUTE LA FRANCE"`
    5. CTAs (HERO-03, HERO-04): Two anchor links in a `flex flex-col gap-4 sm:flex-row` container:
       - Primary: `<a href="#" className={cn(primaryButtonClasses, "px-8 py-4 text-lg")}>` with text via Fr: `"Participez a l'aventure"` — apply the same visual classes as Button variant="primary" size="lg" but as an anchor tag (not a button element). Use inline Tailwind classes matching the Button primary style: `inline-flex items-center justify-center rounded-full font-display font-semibold transition-colors bg-brand-cyan text-white hover:bg-brand-cyan/90 px-8 py-4 text-lg`
       - Secondary: `<a href="#mission" className={...}>` with text via Fr: `"Decouvrir la mission"` — ghost style classes matching Button ghost variant: `inline-flex items-center justify-center rounded-full font-display font-semibold transition-colors bg-transparent border border-white text-white hover:bg-white/10 px-8 py-4 text-lg`
       - Use `<a>` tags (not `<button>`) because these are navigation links — the secondary one scrolls to #mission, the primary one is a placeholder href="#"

    **Important:** Import `Image` from `next/image`, `Fr` from `@/lib/typography`. No client-side JS needed.
  </action>
  <verify>
    <automated>npx tsx -e "import { HeroSection } from './src/components/sections/HeroSection'; console.log(typeof HeroSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: logo, H1 "REIN PADEL TOUR 2026", date badge, two CTAs on blue gradient</manual>
  </verify>
  <done>HeroSection renders logo (priority loaded), "REIN PADEL TOUR 2026" H1, "6 – 15 MARS 2026" badge, primary CTA, and ghost CTA linking to #mission. Blue gradient background, auto height, no 100vh.</done>
</task>

<task type="auto">
  <name>Task 2: Create MissionSection component</name>
  <files>src/components/sections/MissionSection.tsx</files>
  <action>
    Create src/components/sections/MissionSection.tsx as a Server Component.

    **Layout (MISS-01):**
    - Use SectionWrapper with `id="mission"` and `className="bg-white"`
    - Inner grid: `grid grid-cols-1 gap-12 md:grid-cols-2` — text left, fact cards right on desktop; stacked on mobile

    **Left column — text + quote (MISS-02):**
    - Section heading: `<h2>` with `font-display text-3xl font-bold text-brand-navy md:text-4xl` — text via Fr: `"Une maladie invisible. Un defi visible."`
    - Body text (condensed from content doc section 1.2): 2-3 short paragraphs explaining the project. Use Fr component for all French text. Font: `font-body text-brand-gray/80 leading-relaxed`. Condense from content doc:
      - Para 1: "La maladie de Berger touche les reins de maniere silencieuse. Pas de symptomes pendant des annees. Quand on la decouvre, il est souvent deja trop tard."
      - Para 2: "Antoine a 36 ans. Il vit avec cette maladie. Et il a decide de ne plus se taire. Sa methode ? Traverser la France en 10 jours, jouer au padel dans 9 villes, et tracer — par GPS — un rein geant sur la carte de France."
    - Blockquote (MISS-02 — exact text from CONTEXT.md): `<blockquote className="border-l-4 border-brand-coral pl-4 italic text-brand-gray">` containing via Fr: `"« Ma maladie est invisible. Ce tour, c'est ma facon de la rendre visible. »"`, followed by `<cite className="mt-2 block text-sm not-italic text-brand-gray/60">— Antoine</cite>`

    **Right column — fact cards (MISS-03):**
    - Import FACT_NUMBERS from `@/lib/constants`
    - Map over FACT_NUMBERS to render 3 cards in a `grid grid-cols-1 gap-4 sm:grid-cols-3 md:grid-cols-1 lg:grid-cols-3` container
    - Each card: `rounded-xl bg-brand-gray-light p-6 text-center`
      - Value: `font-display text-4xl font-black text-brand-navy`
      - Label: `mt-1 text-sm text-brand-gray/70`
    - Use Fr on labels if they contain French punctuation (they don't in current data, but wrap defensively)

    **Important:** Import SectionWrapper from `@/components/ui/SectionWrapper`, Fr from `@/lib/typography`, FACT_NUMBERS from `@/lib/constants`.
  </action>
  <verify>
    <automated>npx tsx -e "import { MissionSection } from './src/components/sections/MissionSection'; console.log(typeof MissionSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: two-column layout on desktop, quote with coral border, 3 fact cards with correct numbers (9, ~3 000, 10)</manual>
  </verify>
  <done>MissionSection renders with two-column grid (text + blockquote left, 3 fact cards right), responsive stacking on mobile. Antoine's quote in styled blockquote. Fact numbers match CONTEXT.md (9 villes, ~3,000 km, 10 jours).</done>
</task>

</tasks>

<verification>
- `ls src/components/sections/HeroSection.tsx src/components/sections/MissionSection.tsx` both exist
- `grep -c 'priority' src/components/sections/HeroSection.tsx` returns >= 1 (LCP optimization)
- `grep -c 'FACT_NUMBERS' src/components/sections/MissionSection.tsx` returns >= 1 (data from constants)
- `grep '#mission' src/components/sections/HeroSection.tsx` returns match (ghost CTA scrolls to mission)
- `npm run build` passes
</verification>

<success_criteria>
HeroSection and MissionSection are valid React Server Components that can be imported and rendered. Hero has priority-loaded logo and blue gradient. Mission has responsive two-column layout with data from constants.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
