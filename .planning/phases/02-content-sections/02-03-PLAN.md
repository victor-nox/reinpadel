---
phase: 02-content-sections
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/DiseaseSection.tsx
  - src/components/sections/ParticipationSection.tsx
autonomous: true
requirements:
  - DISE-01
  - DISE-02
  - DISE-03
  - PART-01
  - PART-02
  - PART-03

must_haves:
  truths:
    - "Disease section displays white text on dark blue background with verified French statistics"
    - "Disease statistics strip shows 3 impactful numbers with visual emphasis"
    - "Participation section shows 3 cards with distinct button colors (blue, red, outline)"
    - "Each participation card has an icon, title, description, and CTA button"
  artifacts:
    - path: "src/components/sections/DiseaseSection.tsx"
      provides: "Disease awareness section with dark bg and stats strip"
      min_lines: 40
    - path: "src/components/sections/ParticipationSection.tsx"
      provides: "3-card participation section with distinct CTA variants"
      min_lines: 50
  key_links:
    - from: "src/components/sections/DiseaseSection.tsx"
      to: "src/lib/constants.ts"
      via: "DISEASE_STATS import"
      pattern: "import.*DISEASE_STATS.*from.*constants"
    - from: "src/components/sections/ParticipationSection.tsx"
      to: "src/lib/constants.ts"
      via: "PARTICIPATION_CARDS import"
      pattern: "import.*PARTICIPATION_CARDS.*from.*constants"
    - from: "src/components/sections/ParticipationSection.tsx"
      to: "src/components/ui/Button.tsx"
      via: "Button component with variant prop"
      pattern: "variant={card.variant}"
---

<objective>
Build the Disease awareness and Participation section components — the middle sections that communicate medical impact and call visitors to action.

Purpose: Disease section creates emotional urgency (why this matters). Participation section converts that urgency into concrete actions (what you can do). Together they form the persuasion core of the one-pager.
Output: DiseaseSection.tsx and ParticipationSection.tsx, ready for page composition in Plan 05.
</objective>

<execution_context>
@/Users/dominikgrob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dominikgrob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@src/components/ui/Button.tsx
@src/components/ui/SectionWrapper.tsx
@src/lib/typography.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DiseaseSection component</name>
  <files>src/components/sections/DiseaseSection.tsx</files>
  <action>
    Create src/components/sections/DiseaseSection.tsx as a Server Component.

    **Layout (DISE-01):**
    - Use SectionWrapper with `id="maladie"` and `className="bg-brand-navy text-white"` (dark blue bg — DISE-01)
    - DISE-03: white text on brand-navy (#1a3a8a) gives ~9.5:1 contrast ratio — passes WCAG AA and AAA. No special action needed beyond the color classes.

    **Content structure:**
    - Section heading: `<h2>` with `font-display text-3xl font-bold md:text-4xl` — text via Fr: `"La maladie de Berger"`
    - Subheading: `<p>` with `mt-4 max-w-3xl text-lg text-white/80` — text via Fr: `"Une maladie renale chronique. Invisible. Frequente. Et largement meconnue du grand public."`
    - Body text: 1-2 paragraphs condensed from content doc section 3.2 (Qu'est-ce que c'est?). Wrap in `<div className="mt-8 max-w-3xl space-y-4 text-white/80 leading-relaxed">`. Use Fr for each paragraph. Keep it tight — condense the multi-paragraph explanation into 2 concise paragraphs:
      - Para 1: Explain IgA nephropathy simply — immune system deposits attack kidney filters, causing silent progressive damage
      - Para 2: The danger of invisibility — no symptoms for years, discovered late, early screening changes everything

    **Statistics strip (DISE-02):**
    - Import DISEASE_STATS from `@/lib/constants`
    - Render below body text in a `<div className="mt-12 grid grid-cols-1 gap-8 border-t border-white/20 pt-12 sm:grid-cols-3">`
    - Each stat: `text-center` container
      - Value: `font-display text-5xl font-black text-brand-cyan` (cyan for visual pop against navy)
      - Label: `mt-2 text-sm text-white/70`
    - Map over DISEASE_STATS array. Use Fr on labels.
    - Verified numbers from research: ~670 000 / 30% / 0

    **Important:** All French text must use Fr component. No `<a>` links in this section (deferred ideas like AIRG-France links are out of scope for v1 per CONTEXT.md).
  </action>
  <verify>
    <automated>npx tsx -e "import { DiseaseSection } from './src/components/sections/DiseaseSection'; console.log(typeof DiseaseSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: dark blue bg, white text readable, 3 stat numbers in cyan (670 000, 30%, 0)</manual>
  </verify>
  <done>DiseaseSection renders with dark navy background, white text (WCAG AA compliant), explanation of Berger's disease, and 3-stat strip with verified French numbers. All text uses Fr component.</done>
</task>

<task type="auto">
  <name>Task 2: Create ParticipationSection component</name>
  <files>src/components/sections/ParticipationSection.tsx</files>
  <action>
    Create src/components/sections/ParticipationSection.tsx as a Server Component.

    **Layout (PART-01):**
    - Use SectionWrapper with `id="participer"` and `className="bg-white"`
    - Section heading: `<h2>` with `mb-12 text-center font-display text-3xl font-bold text-brand-navy md:text-4xl` — text via Fr: `"Rejoignez l'aventure"`
    - Cards grid: `grid grid-cols-1 gap-8 md:grid-cols-3`

    **Cards (PART-02, PART-03):**
    - Import PARTICIPATION_CARDS from `@/lib/constants`
    - Import Button from `@/components/ui/Button`
    - Each card: `<div className="flex flex-col items-center rounded-2xl border border-brand-gray-light p-8 text-center shadow-sm">`
    - Card content (top to bottom):
      1. Icon: Inline SVG (24x24 or 32x32, currentColor stroke) — NOT an icon library (keeps bundle size zero)
         - 'paddle': Simple padel racket icon — a rounded rectangle with handle and dots pattern. Use a minimal SVG: circle at top (ball) + rounded rect (racket face) + line (handle). Keep simple — this is decorative.
         - 'heart': Standard heart SVG path. Well-known path: `M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z` (viewBox 0 0 24 24, fill currentColor)
         - 'handshake': Two hands meeting — use a simplified SVG. Alternatively, use a partnership/people icon if handshake SVG is too complex. Keep it under 10 lines.
         Render each icon inside a `<div className="mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-brand-gray-light text-brand-navy">` — circular icon container
      2. Title: `<h3 className="mb-2 font-display text-xl font-bold text-brand-navy">` — use Fr on card.title
      3. Description: `<p className="mb-6 flex-1 text-sm text-brand-gray/70 leading-relaxed">` — use Fr on card.description (flex-1 pushes button to bottom)
      4. CTA button (PART-03): `<a href={card.href}><Button variant={card.variant} size="md"><Fr>{card.cta}</Fr></Button></a>` — distinct variants per card: blue, red, outline. Wrap Button in `<a>` for semantic link behavior.

    **Icon rendering approach:** Create a local helper function `function CardIcon({ name }: { name: string })` inside the file that returns the correct inline SVG based on the icon name. Keep SVGs minimal (under 15 lines each). The icons are decorative — they don't need to be pixel-perfect, just recognizable.

    **Important:** Button variant is already a complete Record lookup (blue/red/outline all exist in Button.tsx per Phase 1). No new variants needed.
  </action>
  <verify>
    <automated>npx tsx -e "import { ParticipationSection } from './src/components/sections/ParticipationSection'; console.log(typeof ParticipationSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: 3 cards in row on desktop, stacked on mobile, each with icon + title + text + colored CTA button</manual>
  </verify>
  <done>ParticipationSection renders 3 cards (Jouez/Donnez/Devenez Partenaire) with distinct button variants (blue/red/outline), inline SVG icons, responsive 3-column to single-column layout.</done>
</task>

</tasks>

<verification>
- `ls src/components/sections/DiseaseSection.tsx src/components/sections/ParticipationSection.tsx` both exist
- `grep -c 'DISEASE_STATS' src/components/sections/DiseaseSection.tsx` returns >= 1
- `grep -c 'PARTICIPATION_CARDS' src/components/sections/ParticipationSection.tsx` returns >= 1
- `grep -c 'variant' src/components/sections/ParticipationSection.tsx` returns >= 1 (uses Button variants)
- `npm run build` passes
</verification>

<success_criteria>
DiseaseSection and ParticipationSection are valid React Server Components. Disease section has dark navy bg with white text and verified stat numbers. Participation has 3 cards with distinct Button variant colors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-03-SUMMARY.md`
</output>
