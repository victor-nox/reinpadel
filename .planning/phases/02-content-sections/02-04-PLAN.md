---
phase: 02-content-sections
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/PartnersSection.tsx
  - src/components/sections/FooterSection.tsx
autonomous: true
requirements:
  - PRTN-01
  - PRTN-02
  - PRTN-03
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - FOOT-04
  - FOOT-05

must_haves:
  truths:
    - "Partners section displays all 19 logos at uniform size in a responsive grid"
    - "Partner logos are grayscale by default and show color on hover"
    - "Partners section has a CTA to become a partner"
    - "Footer has 3-column layout with logo+text, navigation links, and social icons"
    - "Footer shows copyright line"
  artifacts:
    - path: "src/components/sections/PartnersSection.tsx"
      provides: "Partner logo grid with grayscale hover effect"
      min_lines: 30
    - path: "src/components/sections/FooterSection.tsx"
      provides: "Dark blue footer with 3-column layout"
      min_lines: 50
  key_links:
    - from: "src/components/sections/PartnersSection.tsx"
      to: "src/lib/constants.ts"
      via: "PARTNERS array import"
      pattern: "import.*PARTNERS.*from.*constants"
    - from: "src/components/sections/PartnersSection.tsx"
      to: "public/partner_logo_*.png"
      via: "next/image with partner.src"
      pattern: "src={.*src}"
    - from: "src/components/sections/FooterSection.tsx"
      to: "public/rpt_logo.png"
      via: "next/image for footer logo"
      pattern: 'src="/rpt_logo.png"'
    - from: "src/components/sections/FooterSection.tsx"
      to: "#mission"
      via: "footer navigation links"
      pattern: 'href="#'
---

<objective>
Build the Partners logo grid and Footer section components — the bottom sections that showcase supporters and provide site-wide navigation and legal links.

Purpose: Partners section builds credibility (who supports the project). Footer provides closure with navigation, social links, and legal compliance.
Output: PartnersSection.tsx and FooterSection.tsx, ready for page composition in Plan 05.
</objective>

<execution_context>
@/Users/dominikgrob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dominikgrob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@src/components/ui/Button.tsx
@src/components/ui/SectionWrapper.tsx
@src/lib/typography.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PartnersSection component</name>
  <files>src/components/sections/PartnersSection.tsx</files>
  <action>
    Create src/components/sections/PartnersSection.tsx as a Server Component.

    **Layout:**
    - Use SectionWrapper with `id="partenaires"` and `className="bg-brand-gray-light"`
    - Section heading: `<h2>` with `mb-10 text-center font-display text-3xl font-bold text-brand-navy md:text-4xl` — text via Fr: `"Nos partenaires"`
    - Intro text: `<p className="mx-auto mb-10 max-w-2xl text-center text-brand-gray/70">` — condensed from content doc section 9.1: `"La Rein Padel Tour 2026 existe grace a des partenaires qui croient en notre mission."` via Fr

    **Logo grid (PRTN-01 — CONTEXT.md override: all 19 at same size, NO two-tier distinction):**
    - Import PARTNERS from `@/lib/constants`
    - Grid: `grid grid-cols-3 gap-6 sm:grid-cols-4 md:grid-cols-5 lg:grid-cols-6` — responsive columns, all logos same size
    - Each logo container: `<div className="flex items-center justify-center rounded-lg bg-white p-4 shadow-sm">`
    - Image: `<Image src={p.src} alt={p.alt} width={120} height={60} className="h-10 w-auto object-contain grayscale transition-all duration-300 hover:grayscale-0" />`

    **Grayscale hover (PRTN-02):**
    - Tailwind v4 includes `grayscale` and `grayscale-0` utilities natively
    - `className="... grayscale transition-all duration-300 hover:grayscale-0"` on each Image
    - This applies CSS `filter: grayscale(100%)` and removes it on hover — pure CSS, zero JS

    **CTA (PRTN-03):**
    - Below the grid: `<div className="mt-10 text-center">`
    - `<a href="#"><Button variant="outline"><Fr>{"Devenir partenaire"}</Fr></Button></a>`
    - Placeholder href="#" per CONTEXT.md (no external links in v1)

    **Note on partner logo dimensions:** Use `width={120} height={60}` as safe defaults. next/image will render at CSS-controlled size (`h-10 w-auto`). If any logos look distorted, this is a minor visual polish issue — the `object-contain` class prevents actual distortion. The Plan 01 SUMMARY will contain actual intrinsic dimensions if the executor captured them.
  </action>
  <verify>
    <automated>npx tsx -e "import { PartnersSection } from './src/components/sections/PartnersSection'; console.log(typeof PartnersSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: grid of 19 logos at same size, grayscale by default, color on hover, CTA button below</manual>
  </verify>
  <done>PartnersSection renders 19 partner logos at uniform size in a responsive grid. Logos are grayscale by default, showing color on hover (CSS only). CTA button "Devenir partenaire" below the grid.</done>
</task>

<task type="auto">
  <name>Task 2: Create FooterSection component</name>
  <files>src/components/sections/FooterSection.tsx</files>
  <action>
    Create src/components/sections/FooterSection.tsx as a Server Component.

    **Layout (FOOT-01):**
    - Use a `<footer>` element (semantic HTML — PERF-04) with `className="bg-brand-navy text-white"`
    - Inner container: `<div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-12">`
    - Do NOT use SectionWrapper — footer has its own semantic element and different padding
    - 3-column grid: `grid grid-cols-1 gap-10 md:grid-cols-3`

    **Column 1 — Logo + mission statement (FOOT-02):**
    - Logo: `<Image src="/rpt_logo.png" alt="Rein Padel Tour" width={424} height={424} className="h-12 w-12 object-contain mb-4" />`
    - Mission text: `<p className="text-sm text-white/70 leading-relaxed">` — via Fr: `"Tournois amicaux caritatifs a travers la France pour sensibiliser a la maladie de Berger."` (condensed brand tagline)

    **Column 2 — Navigation links (FOOT-03):**
    - `<nav aria-label="Navigation du pied de page">`
    - `<ul className="space-y-2 text-sm">`
    - Links using anchor hrefs to section IDs:
      - `<a href="#mission">La mission</a>`
      - `<a href="#maladie">La maladie de Berger</a>`
      - `<a href="#participer">Participer</a>`
      - `<a href="#partenaires">Nos partenaires</a>`
      - `<a href="/mentions-legales">Mentions legales</a>` (page created in Phase 4 — link works now, 404 until then)
    - Link styling: `text-white/70 hover:text-white transition-colors`
    - Use Fr on link text where punctuation applies (most don't have special punctuation)

    **Column 3 — Social icons + copyright (FOOT-04, FOOT-05):**
    - Social icons in a `<div className="flex gap-4 mb-6">`:
      - Instagram: `<a href="#" aria-label="Instagram" className="text-white/70 hover:text-white transition-colors">` with inline SVG icon (24x24). Use the standard Instagram camera SVG path: rounded rectangle with circle lens and dot.
      - Facebook: `<a href="#" aria-label="Facebook" className="text-white/70 hover:text-white transition-colors">` with inline SVG icon (24x24). Use the standard Facebook 'f' SVG path.
      - Both use placeholder href="#" (no external links in v1)
      - SVG size: `className="h-6 w-6"` with `fill="none" stroke="currentColor" strokeWidth="2"` (outline style) or `fill="currentColor"` (solid style) — choose whichever looks cleaner

    - Copyright (FOOT-05): `<p className="text-xs text-white/50">` — text: `"© 2026 Rein Padel Tour. Tous droits reserves."` via Fr

    **Important:** Inline SVGs for social icons — do NOT install an icon library. Two icons is not worth adding a dependency. Keep each SVG under 10 lines.
  </action>
  <verify>
    <automated>npx tsx -e "import { FooterSection } from './src/components/sections/FooterSection'; console.log(typeof FooterSection === 'function' ? 'PASS' : 'FAIL')"</automated>
    <manual>Visually check: dark blue footer, 3 columns on desktop, logo, nav links, social icons, copyright</manual>
  </verify>
  <done>FooterSection renders with dark navy background, 3-column grid (logo+tagline, section links, social icons + copyright). Semantic footer element with aria-labeled nav. Social icons are inline SVGs.</done>
</task>

</tasks>

<verification>
- `ls src/components/sections/PartnersSection.tsx src/components/sections/FooterSection.tsx` both exist
- `grep -c 'PARTNERS' src/components/sections/PartnersSection.tsx` returns >= 1
- `grep -c 'grayscale' src/components/sections/PartnersSection.tsx` returns >= 1 (hover effect)
- `grep -c 'footer' src/components/sections/FooterSection.tsx` returns >= 1 (semantic element)
- `grep -c 'aria-label' src/components/sections/FooterSection.tsx` returns >= 2 (social icons + nav)
- `npm run build` passes
</verification>

<success_criteria>
PartnersSection and FooterSection are valid React Server Components. Partners grid shows 19 logos with grayscale-to-color hover. Footer has semantic HTML, 3-column layout, social icons, and copyright.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-04-SUMMARY.md`
</output>
